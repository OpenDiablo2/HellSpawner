# HellSpawner

The OpenDiablo2 toolset

## Prequesites (MacOS)

- brew
- go v1.14.x

## Get Started (MacOS)

1. `go mod vendor`
2. `brew install pkg-config`
3. `brew install glib`
4. `brew install gtk+3`
5. `go run main.go`
